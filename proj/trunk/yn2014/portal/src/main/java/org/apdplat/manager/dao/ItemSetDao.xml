<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.apdplat.manager.dao.ItemSetDao">

<select id="listItem" resultType="java.util.Map">
     SELECT index_id as "id",-1 as "pId",index_name as "name",'false' as "isParent" FROM PMRT.TAB_MRT_CHNL_INDEX WHERE LEV=1
</select>

<select id="getCheckIds" parameterType="java.util.Map" resultType="java.util.Map">
     SELECT INDEX_ID FROM PMRT.TAB_MRT_INDEX_DEPLOY_MON WHERE DEAL_DATE=#{dealDate} AND GROUP_ID_1=#{group_id_1}
</select>

<delete id="delete" parameterType="java.util.Map">
     DELETE FROM PMRT.TAB_MRT_INDEX_DEPLOY_MON WHERE DEAL_DATE=#{dealDate} AND GROUP_ID_1=#{group_id_1} AND INDEX_ID=#{id}
</delete>
</mapper>